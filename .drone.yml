build:
  image: teaci/msys$$arch
  pull: true
#  shell: msys$$arch
  commands:
    - if [ "$$arch" = 32 ]; then true; else false; fi

matrix:
  runtest:
    - false
  arch:
    - 64
#    - 32
# Temprorary disable matrix build to workaround irc plugin bug on wine-ci.org, see https://github.com/drone/drone/issues/1459
#    - true # allow failures

notify:
  gitter:
    webhook: https://webhooks.gitter.im/e/d8f2032e40a8e78a3882
